<!DOCTYPE html>
<html lang="en" dir="ltr">
<head>
  <meta charset="utf-8">
  <title>Sinatra Brokerage Investments</title>
</head>
<body>
  <nav>
    <% if logged_in? %>

    <a href="/accounts/new"> Open a Brokerage Account</a>
    <a href="/logout"> Log Out </a>
    <p>Welcome, <%=current_user.username %></p>

    <% else %>
    <a href="/signup">Sign Up</a>
    <a href="/login"> Sign In </a>
    <%end %>
  </nav>

  <div class="container">
    <h1>Account Stock Portfolio</h1>
    <% if !@error_message.nil? %>
    <div class="alert alert-warning">
      <%= @error_message %>
    </div>
    <% end %>
    <%= yield %>
  </div>
</body>
</html>
