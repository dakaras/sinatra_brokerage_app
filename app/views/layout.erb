<!DOCTYPE html>
<html lang="en" dir="ltr">
<head>
  <meta charset="utf-8">
  <title>Sinatra Brokerage Investments</title>
</head>
<body>
  <%= yield %>

  <nav>
    <% if logged_in? %>

    <a href="/accounts/new"> Open a Brokerage Account</a>
    <a href="/logout"> Log Out </a>
    <p>Welcome, <%=current_user.username %></p>

    <% end %>
  </nav>

  <div class="container">
    <% if logged_in? %>
    <h2>Account Stock Portfolio</h2>
    <% end %>

  </div>
</body>
</html>
